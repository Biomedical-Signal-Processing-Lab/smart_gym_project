# ───────── Core numerical & signal processing ─────────
numpy>=1.23.0
scipy>=1.10.0
joblib>=1.3.0

# ───────── BLE communication (for Arduino Nano 33 IoT) ─────────
bleak>=0.22.1

# ───────── Utility & logging ─────────
asyncio           # 표준모듈이지만, 일부 환경에서 별도 설치 필요 시
argparse          # 표준모듈 (명시만)
tqdm>=4.65.0      # (선택) 진행 표시용 - 디버깅 시 유용

# ───────── File I/O and optional visualization ─────────
pandas>=2.0.0     # (선택) 로그 분석 및 TSV 확인용
matplotlib>=3.7.0 # (선택) 데이터 시각화용

# ───────── OS-level dependencies ─────────
# 라즈베리파이에서 BLE 작동을 위해 필요한 시스템 패키지:
# sudo apt install bluetooth bluez libbluetooth-dev


numpy>=1.23.0
scipy>=1.10.0
joblib>=1.3.0
bleak>=0.22.1
scikit-learn>=1.3,<1.6